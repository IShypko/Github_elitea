import { test, expect } from '@playwright/test';

// Skeleton: test steps will be added in a follow-up edit to keep changes incremental
test('Verify Client Work text is visible on EPAM site - skeleton', async ({ page }) => {
  // TODO: add navigation and assertions
});

{
  "name": "playwright-tests",
  "version": "1.0.0",
  "description": "Playwright tests for EPAM site - automated by agent",
  "scripts": {
    "test": "npx playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.34.0"
  }
}